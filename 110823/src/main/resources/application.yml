server:
  port: 8081
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/hr
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    